import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { QueryProvider } from "@/providers/query-provider";
import { HydrationProvider } from "@/providers/hydration-provider";
import { ThemeProvider } from "@/providers/theme-provider";
import { ThemeColorBridge } from "@/providers/theme-color-bridge";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "POS Studio",
  description: "Mobile-first POS and business management",
  viewport: {
    width: "device-width",
    initialScale: 1,
    maximumScale: 1,
    viewportFit: "cover",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
      suppressHydrationWarning
    >
      <head>
        <meta name="theme-color" content="#12080a" />
        <link
          rel="stylesheet"
          href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@24,400,0,0&display=swap"
        />
      </head>
      <body className="min-h-full flex flex-col bg-stitch-bg text-stitch-fg">
        <ThemeProvider>
          <ThemeColorBridge />
          <HydrationProvider>
            <QueryProvider>{children}</QueryProvider>
          </HydrationProvider>
        </ThemeProvider>
      </body>
    </html>
  );
}
